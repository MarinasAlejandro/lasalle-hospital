[project]
name = "hospital-pipeline"
version = "0.1.0"
description = "Sistema Inteligente de Soporte Hospitalario — laSalle Health Center"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
python_files = ["test_*.py"]
